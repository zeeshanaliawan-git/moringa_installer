# Format :
# Parametres g�neraux : sans prefixe
# Parametres de classe prefix�s par classe_ ( majus ).
# Param�tres de classe supplantent parametres g�n�raux.
# Les classes sont instanci�es sur WITH_TAG = (OoYySs1) .

CONNECT jdbc:mariadb://127.0.0.1/dev_commons?user=devdb&password=
CONNECT_DRIVER org.mariadb.jdbc.Driver

This folder contains all those js files which we don't want to be cached. 
These are our local path files and has some ajax calls so instead of caching and making ajax calls go through cache mechanism we want them to be called directly. 
So we will ignore all the js in this folder.